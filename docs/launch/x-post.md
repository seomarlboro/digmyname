# X (Twitter)

## Thread draft

**1/**
Built a domain checker that does two things differently:

→ 54 pages showing what registrars actually charge to renew, not just the $0.99 headline
→ says "Unverified" instead of guessing when its 3 signals disagree

Free, no signup. First answer under 0.5s (p95, measured — or dispute it): https://digmyname.com

**2/**
Renewal traps are the dirty secret of domain pricing — $1 year one, $60 year two, and most checkers never show you that until checkout. Every extension gets its own page with real numbers and a verification date: https://digmyname.com/tld

**3/**
Also ships as a free JSON API and an MCP server (`domain-check-skills-mcp`) so Claude, Cursor or any LLM can check domains directly — no key, no auth.

## Notes for the owner

- Attach a screenshot or the demo gif to tweet 1 (see [demo-script.md](./demo-script.md)) — plain-text tweets underperform for launches.
- Post after the Show HN / Product Hunt submission is live and linkable, so replies can point to the traction rather than a cold link.
- Pin tweet 1 for launch day.
